Small, deterministic sample files used by automated tests.

Guidelines:
- Keep files tiny (<10KB) and under version control
- Prefer CSV with 2-3 columns and 20-200 rows
- Include a matching manifest if used in provenance tests
